% This file will be generated by a makefile from nagios cfg.
{target,
    'hostTest1',                            % id
    {perm_conf,     ["admin"], ["admin"]},  % global_perm
    [{ip, ["173.194.34.55"]}],              % properties
    [                                       % probes
        {probe,
            1,                                  % id -> makefile gen
            undefined,                          % pid
            'check_icmp-1',                     % name -> makefile gen      
            {perm_conf, ["admin"], ["admin"]},  % permissions
            btracker_probe_nagios,              % tracker_probe_mod
            {nagios_plugin,                     % tracker_probe_conf
                "/opt/nagios-plugins-1.4.16/libexec/check_icmp",
                [{"-H", "173.194.34.55"}, {"-t", "5"}]},
            'UNKNOWN',                          % status -> default,
            5,                                  % timeout,
            4,                                  % step,
            status,                             % type -> default,
            [],                                 % inspectors -> default,
            %[{logger, 
                %btracker_logger_file, []}],     % loggers -> default,
            [],
            1,                                  % active  -> default,
            []                                  % oid
        }
    ],
    "var/tracker/check_icmp-1"                  % directory -> makefile gen
}.
