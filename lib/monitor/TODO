0 add ref() to probe_nchecks (race condition with send_after())
1 dependencies/alertes apres x CRITICAL (and remove inspectors)
2 template system
    - for probes
    - for snmp
    - events (scheduler/alerts)
3 general code cleanup/refactor/test (eunit/dializer)

#
4 monitor_data_master should not lock on client synchro. Fecth from another
data source (SQL, other), and return immediately.
5 a target and his probe can be abstract to be something else than a network host
